<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>VIT Complaint Portal</title>
    <link rel="stylesheet" type="text/css" href="complaint.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Serif+Display:wght@300&display=swap" rel="stylesheet">
</head>
<body>
    <div class ="main">
        <div class ="title">
            <img src = "https://vtop.vit.ac.in/vtop/assets/img/VITLogoEmblem.png">
            <h1>VIT Complaint Portal</h1>
        </div>
        <div class="complaint">
            <h1>Complaint details</h1>
            <table class="table">
                <thead>
                    <tr>
                        <td>Name</td>
                        <td>Room No</td>
                        <td>Type</td>
                        <td>Description</td>
                        <td>Status</td>
                    </tr>
                </thead>
                <tbody>
                    <% complaints.forEach(complaint => { %>
                        <tr>
                            <td><%= complaint.name %></td>
                            <td><%= complaint.room %></td>
                            <td><%= complaint.nature %></td>
                            <td class="desp"><%= complaint.desp %></td>
                            <td class="<%= complaint.status === 'open' ? 'open-status' : '' %>"><%= complaint.status ? 'closed' : 'open' %></td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>
